node_modules
# Keep environment variables out of version control
.env
CLAUDE.md

/generated/prisma

# Local Python deps used for tooling/scripts
.pydeps/
.DS_Store
# Local Python virtual environment
.venv/

# Downloaded Transformers.js embedding model weights (see backend/src/services/embeddings.ts)
backend/.cache/

# Local-only tooling/scratch artifacts (never commit these)
.claude/agent-memory/
.cursor/
.shots/
.v2preview/
dist/

# Root-level package.json was created by installing deps at the repo root by
# mistake (the app deps live in frontend/). Keep it out of version control.
/package.json