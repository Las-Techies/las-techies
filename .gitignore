node_modules
# Keep environment variables out of version control
.env
CLAUDE.md

/generated/prisma

# Local Python deps used for tooling/scripts
.pydeps/
.DS_Store
# Local Python virtual environment
.venv/

# Downloaded Transformers.js embedding model weights (see backend/src/services/embeddings.ts)
backend/.cache/

# Local-only tooling/scratch artifacts (never commit these)
.claude/agent-memory/
.cursor/
.shots/
.v2preview/
dist/

# transitions-refine (npx transitions-refine live) local relay state —
# points at localhost, never meant to be committed. `frontend/index.html`'s
# injected <script> tag is stripped automatically by `transitions-refine
# stop` and shouldn't be committed either.
.agents/
.refine-relay.json
.refine-relay.log